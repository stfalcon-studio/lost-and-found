{% extends 'frontend/user/profile.html.twig' %}

{% import 'frontend/user/macro.html.twig' as user_macro %}

{% block title %}
    Actual lost items |
    {{ parent() }}
{% endblock title %}

{% block content %}
    {{ user_macro.active_tab('lost_items') }}

    {{ user_macro.active_pill('lost_items', 'actual') }}

    <br />
    {% if items is defined and items is not empty %}
        {{ user_macro.actual_items_list(items) }}
    {% endif %}
{% endblock content %}
