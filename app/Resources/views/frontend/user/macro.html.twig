{% macro actual_items_list(items) %}
    <table class="table table-bordered table-hover">
        <tbody>
            {% for item in items %}
                <tr>
                    <td>
                        <a href="{{ path('item_details', {'id': item.id}) }}">{{ item.title }}</a>
                    </td>
                    <td>
                        <a href="{{ path('item_edit', {'id': item.id}) }}">Edit</a>
                    </td>
                    <td>
                        <a href="{{ path('item_deactivate', {'id': item.id}) }}">Deactivate</a>
                    </td>
                </tr>
            {% endfor %}
            <b></b>
        </tbody>
    </table>
{% endmacro actual_items_list %}

{% macro deactivated_items_list(items) %}
    <table class="table table-bordered table-hover">
        <tbody>
            {% for item in items %}
                <tr>
                    <td>
                        <a href="{{ path('item_details', {'id': item.id}) }}">{{ item.title }}</a>
                    </td>
                    <td>
                        <a href="{{ path('item_delete', {'id': item.id}) }}">Delete</a>
                    </td>

                    <td>
                        <a href="{{ path('item_activate', {'id': item.id}) }}">Activate</a>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
{% endmacro deactivated_items_list %}

{% macro not_moderated_items_list(items) %}
    <table class="table table-bordered table-hover">
        <tbody>
            {% for item in items %}
                <tr>
                    <td>
                        <a href="{{ path('item_details', {'id': item.id}) }}">{{ item.title }}</a>
                    </td>
                    <td>
                        <a href="{{ path('item_edit', {'id': item.id}) }}">Edit</a>
                    </td>
                    <td>
                        <a href="{{ path('item_delete', {'id': item.id}) }}">Delete</a>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
{% endmacro not_moderated_items_list %}

{% macro resolved_items_list(items) %}
    <table class="table table-bordered table-hover">
        <tbody>
            {% for item in items %}
                <tr>
                    <td>
                        <a href="{{ path('item_details', {'id': item.id}) }}">{{ item.title }}</a>
                    </td>
                    <td>
                        ToDo
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
{% endmacro resolved_items_list %}

{% macro active_tab(active_tab, count) %}
    <ul class="nav nav-tabs">
        <li role="presentation" {% if active_tab == 'lost_items' %}class="active"{% endif %}>
            {% if active_tab == 'lost_items' %}
                <a href="#">My lost items {% if count.lost > 0 %}[{{count.lost}}]{% endif %}</a>
            {% else %}
                <a href="{{ path('user_actual_lost_items') }}">My lost items {% if count.lost > 0 %}[{{count.lost}}]{% endif %}</a>
            {% endif %}
        </li>

        <li role="presentation" {% if active_tab == 'found_items' %}class="active"{% endif %}>
            {% if active_tab == 'found_items' %}
                <a href="#">My found items {% if count.found > 0 %}[{{count.found}}]{% endif %}</a>
            {% else %}
                <a href="{{ path('user_actual_found_items') }}">My found items {% if count.found > 0 %}[{{count.found}}]{% endif %}</a>
            {% endif %}
        </li>
        <li role="presentation" {% if active_tab == 'deactivated_items' %}class="active"{% endif %}>
            {% if active_tab == 'deactivated_items' %}
                <a href="#">My deactivated items {% if count.notActive > 0 %}[{{count.notActive}}]{% endif %}</a>
            {% else %}
                <a href="{{ path('user_deactivated_items') }}">My deactivated items {% if count.notActive > 0 %}[{{count.notActive}}]{% endif %}</a>
            {% endif %}
        </li>
        <li role="presentation" {% if active_tab == 'not_moderated_items' %}class="active"{% endif %}>
            {% if active_tab == 'not_moderated_items' %}
                <a href="#">My not moderated items {% if count.notModerated > 0 %}[{{count.notModerated}}]{% endif %}</a>
            {% else %}
                <a href="{{ path('user_not_moderated_items') }}">My not moderated items {% if count.notModerated > 0 %}[{{count.notModerated}}]{% endif %}</a>
            {% endif %}
        </li>
    </ul>
{% endmacro active_tab %}

{% macro active_pill(active_tab, active_pill) %}
    <ul class="nav nav-pills">
        <li role="presentation" {% if active_pill == 'actual' %}class="active"{% endif %}>
            {% if active_pill == 'actual' %}
                <a href="#">Actual</a>
            {% else %}
                {% if active_tab == 'found_items' %}
                    <a href="{{ path('user_actual_found_items') }}">Actual</a>
                {% elseif active_tab == 'lost_items' %}
                    <a href="{{ path('user_actual_lost_items') }}">Actual</a>
                {% endif %}
            {% endif %}
        </li>
        <li role="presentation" {% if active_pill == 'resolved' %}class="active"{% endif %}>
            {% if active_pill == 'resolved' %}
                <a href="#">Resolved</a>
            {% else %}
                {% if active_tab == 'found_items' %}
                    <a href="{{ path('user_resolved_found_items') }}">Resolved</a>
                {% elseif active_tab == 'lost_items' %}
                    <a href="{{ path('user_resolved_lost_items') }}">Resolved</a>
                {% endif %}
            {% endif %}
        </li>
    </ul>
{% endmacro active_pill %}
