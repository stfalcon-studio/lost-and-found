{% extends 'frontend/user/profile.html.twig' %}

{% import 'frontend/user/macro.html.twig' as user_macro %}

{% block title %}
    Resolved found items |
    {{ parent() }}
{% endblock title %}

{% block content %}
    {{ user_macro.active_tab('found_items', count) }}

    {{ user_macro.active_pill('found_items', 'resolved') }}

    <br />
    {% if items is defined and items is not empty %}
        {{ user_macro.resolved_items_list(items) }}
    {% endif %}
{% endblock content %}
