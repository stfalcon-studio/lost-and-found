parameters:
    application_user.user.provider.class: AppBundle\Security\Provider\AuthProvider

services:
    application_user.user.provider:
        class: %application_user.user.provider.class%
        arguments: [@fos_user.user_manager, {github: facebookId}]